import streamlit as st
st.set_page_config(page_title="Metrics Q&A")
st.title("Metrics Q&A (CSV → LLM)")
st.write("Upload a CSV, ask a question, and see an answer with cited columns.")
st.caption("Skeleton app; wiring to model/validation comes next.")
